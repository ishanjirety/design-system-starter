import styled from 'styled-components'
import {
    flexbox,
    space,
    layout,
    position,
    color,
    border,
    typography
} from 'styled-system'

export const Input = styled.input<InputTypes>`
    ${flexbox}
    ${space}
    ${layout}
    ${position}
    ${color}
    ${border}
    ${typography}
`
