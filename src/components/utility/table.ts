import styled from 'styled-components'
import {
    border,
    space,
    color,
    layout,
    shadow,
    typography,
    grid,
    flexbox
} from 'styled-system'

export const Table = styled.table<TableTypes>`
    ${color}
    ${space}
    ${layout}
    ${border}
    ${shadow}
    ${typography}
    ${flexbox}
    ${grid}
`


