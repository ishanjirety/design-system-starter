import styled from 'styled-components'
import {
    flexbox,
    space,
    layout,
    color,
    border,
    typography
} from 'styled-system'

export const TextArea = styled.textarea<InputTypes>`
    ${flexbox}
    ${space}
    ${layout}
    ${color}
    ${border}
    ${typography}
`


